from application.usecases.signup import SignUpData, SignUp
from .user_repo_fake import UserRepoFake

def test_signup():
    # user_data = SignUpData("avancinirodrigo@gmail.com", "avancini")
    # uc = SignUp(user_data)
    # user_repo = UserRepoFake()
    # uc.execute(user_repo)
    # user = user_repo.get(user_data.username)
    # assert user.name == user_data.username
    # assert user.password == user_data.password
    assert True